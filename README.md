```
usage: python3 commands.py <COMMAND> -a <value> -n <value> -r [-h]

Script description

positional arguments:
  command               Executes some command

optional arguments:
  -h, --help            show this help message and exit
  -a <value>, --argument <value>
                        First argument
  -n Some Name, --name Some Name
                        Name used inside script
  -r, --run             Flag used to run extra instruction
```